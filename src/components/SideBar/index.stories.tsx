import type { Meta, StoryObj } from '@storybook/react';
import SideBar from '.';

const meta: Meta = {
  title: 'Components/SideBar',
  component: SideBar,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const SideBarObj: Story = {};
