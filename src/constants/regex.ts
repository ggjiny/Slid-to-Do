export const VALID_URL_REGEX =
  /^(ftp|http|https):\/\/(?:[a-zA-Z0-9-]+\.)+[a-zA-Z]{2,}(?:\/[^\s]*)?$/;
export const VALID_MAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const TAG_REGEX = /(<([^>]+)>)/gi;
